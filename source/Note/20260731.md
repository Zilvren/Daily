## 问题

短信管理，只能给出日志的接口，若给出实时查询，主数据库会高负载，所以想要实现功能，必须做一个定时任务用接口在晚上查，写入自己数据库的表(chaojijiaolian.sms_log)里，另一个定时任务做5分钟查一次的，也写入数据库表，保证实时统计的内容是基本正确，点页面接口查询，就能查一定数量的保证近一段时间能查到，这部分不写入数据库



评论管理，日期控件开始时间和结束时间切换时下拉框没变，导出命名不规范



